% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.umg}
\alias{plot.umg}
\title{Plot a UMG with grid graphics}
\usage{
\method{plot}{umg}(
  x,
  theme = umg_theme(),
  parse_labels = NULL,
  node_r = NULL,
  orientation = NULL,
  ...
)
}
\arguments{
\item{x}{An object of class \code{umg} (layout computed automatically if
absent).}

\item{theme}{A \code{umg_theme} object (default \code{\link[=umg_theme]{umg_theme()}}), or a
style name (\code{"journal"}, \code{"slide"}, \code{"cb"}).}

\item{parse_labels}{Logical; parse labels as plotmath. Defaults to
the theme value. TeX \code{$} delimiters are stripped for display.}

\item{node_r}{Vertex radius in native units; defaults to the theme
value.}

\item{orientation}{Layer direction passed to \code{\link[=umg_layout]{umg_layout()}}:
\code{"TB"} (top to bottom, default) or \code{"LR"} (left to right).}

\item{...}{Passed to \code{\link[=umg_layout]{umg_layout()}} when a layout is absent.}
}
\value{
Invisibly \code{x} (with layout attached).
}
\description{
Renders the diagram using the same channel allocation as the TikZ
lexicon: fill encodes observability, shape encodes support, border
and dedicated shapes encode role, and plates are rounded rectangles.
Appearance is governed by a theme (see \code{\link[=umg_theme]{umg_theme()}}); individual
vertices may override their fill by carrying a \code{fill} element.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duality.R
\name{umg_caterpillar}
\alias{umg_caterpillar}
\title{Caterpillar (shrinkage) plot of cluster-level estimates}
\usage{
umg_caterpillar(
  estimates,
  se = NULL,
  level = 0.95,
  title = "Caterpillar plot: cluster estimates"
)
}
\arguments{
\item{estimates}{A numeric vector of cluster-level estimates, or a
data frame with a column \code{estimate} and optionally \code{se} and
\code{group}.}

\item{se}{Optional numeric vector of standard errors (used when
\code{estimates} is a numeric vector).}

\item{level}{Confidence level for the interval (default \code{0.95}).}

\item{title}{Plot title.}
}
\value{
A ggplot object.
}
\description{
Renders the caterpillar plot implied by a random-coefficient
vertex: cluster-level estimates ordered by value with optional
uncertainty intervals, the canonical display for inspecting
between-cluster variation and shrinkage. The input is the estimated
deviations, which the modelling package supplies after fitting;
the diagram object names the display, this function draws it.
}
\examples{
set.seed(1)
if (requireNamespace("ggplot2", quietly = TRUE))
  umg_caterpillar(rnorm(20), se = runif(20, 0.2, 0.5))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/identify.R
\name{umg_check_scaling}
\alias{umg_check_scaling}
\title{Check latent-variable scaling marks}
\usage{
umg_check_scaling(model)
}
\arguments{
\item{model}{An object of class \code{umg}.}
}
\value{
A data frame with one row per latent continuous random
vertex and columns \code{vertex}, \code{scaled} (logical), \code{via} (the
scaling mechanism detected: \code{"marker loading"}, \code{"fixed variance"}, \code{"marker weight"}, or \code{"none"}), and
\code{location_fixed} (logical; informational, \code{TRUE} when a constant
parent fixes the location).
}
\description{
Every latent continuous random vertex must have its scale fixed,
either by a unit-valued outgoing loading (marker method), by a
fixed variance (variance-standardisation method), or, for a
formative composite, by a unit-valued incoming weight from an
observed cause (the marker convention for composites). This function
inspects each such vertex and reports whether a scaling mark is
present, exposing the single most common specification error in
latent variable modelling as a missing mark rather than a
nonconvergence.
}
\details{
A constant parent (a \code{const} vertex with an edge into the latent
vertex) fixes the \emph{location} of the latent variable, not its scale:
it pins the mean, and a variance can still be traded against the
loadings. It is therefore reported separately in \code{location_fixed}
and does not count as a scaling mark.
}
\examples{
umg_check_scaling(umg_factor("F", paste0("y", 1:3)))
umg_check_scaling(umg_formative(paste0("x", 1:3), outcomes = c("y1", "y2")))
}

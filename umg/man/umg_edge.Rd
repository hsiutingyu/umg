% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructors.R
\name{umg_edge}
\alias{umg_edge}
\title{Create a UMG edge}
\usage{
umg_edge(
  from,
  to,
  kind = c("dep", "cov", "det", "mix"),
  label = "",
  fixed = NULL,
  aggregate = FALSE
)
}
\arguments{
\item{from, to}{Vertex names (character scalars).}

\item{kind}{Edge kind: \code{"dep"} (stochastic dependence), \code{"cov"}
(symmetric covariance), \code{"det"} (deterministic assignment), or
\code{"mix"} (mixture selection from a categorical vertex).}

\item{label}{Optional parameter label (e.g., \code{"$\\\\lambda_2$"}); an
edge label is shorthand for a parameter vertex (parameter
promotion; see the accompanying article, Section 4.5).}

\item{fixed}{Optional fixed value (e.g., \code{1} for a scaling loading).}

\item{aggregate}{Logical; \code{TRUE} marks a deterministic (\code{det}) edge
as an aggregation over a replicated index, for example a cluster
mean computed from occasion-level observations. Such an edge runs
from a vertex inside a plate to a vertex outside it, which the
index-licensing rule W5 would otherwise refuse (see
\code{\link[=umg_validate]{umg_validate()}}); marking it \code{aggregate = TRUE} exempts it from
that rule. Only meaningful for \code{det} edges; default \code{FALSE}.}
}
\value{
An object of class \code{umg_edge}.
}
\description{
Create a UMG edge
}
\examples{
umg_edge("eta1", "y1", kind = "dep", fixed = 1)
umg_edge("eta1", "eta2", kind = "cov", label = "$\\\\psi_{21}$")
umg_edge("y", "ybar", kind = "det", aggregate = TRUE)
}

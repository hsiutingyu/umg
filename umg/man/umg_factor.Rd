% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifs.R
\name{umg_factor}
\alias{umg_factor}
\title{Reflective factor (measurement) motif}
\usage{
umg_factor(
  factor = "F",
  indicators = c("y1", "y2", "y3"),
  loadings = NULL,
  factor_label = NULL,
  indicator_labels = NULL,
  plate_index = "i = 1, ..., N",
  dist = "N(0, psi)"
)
}
\arguments{
\item{factor}{Name of the latent factor (character scalar).}

\item{indicators}{Character vector of indicator names (>= 2).}

\item{loadings}{Optional character vector of loading labels for the
free loadings (the indicators after the first), so of length
\code{length(indicators) - 1}. A vector of length \code{length(indicators)}
is also accepted, in which case its first element (the fixed
marker loading) is ignored. Defaults to \code{lambda[k]}.}

\item{factor_label, indicator_labels}{Optional display labels.}

\item{plate_index}{Index label for the person plate.}

\item{dist}{Distribution annotation for the factor (source vertex).}
}
\value{
An object of class \code{umg}.
}
\description{
Builds a single-factor reflective measurement model: one latent
continuous factor with directed loadings onto observed continuous
indicators, the first loading fixed to 1 for scaling, residual
variances implied, and a person plate.
}
\examples{
m <- umg_factor("F", c("y1", "y2", "y3"))
plot(m)
}

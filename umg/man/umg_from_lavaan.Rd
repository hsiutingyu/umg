% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{umg_from_lavaan}
\alias{umg_from_lavaan}
\title{Build a UMG from a lavaan model}
\usage{
umg_from_lavaan(object, plate_index = "i = 1, ..., N", bayesian = FALSE)
}
\arguments{
\item{object}{A fitted lavaan object, or a lavaan model syntax
string (which is then lavaanified without fitting).}

\item{plate_index}{Index label for the person plate.}

\item{bayesian}{Logical; if \code{TRUE}, free parameters are rendered in
their prior-closed (Bayesian) form. Defaults to \code{FALSE}. Used by
\code{\link[=umg_from_blavaan]{umg_from_blavaan()}}.}
}
\value{
An object of class \code{umg}.
}
\description{
Translates a lavaan parameter table into a UMG: latent variables
(\verb{=~} left-hand sides) become latent continuous vertices, observed
indicators become observed continuous vertices, loadings and
regressions become \code{dep} edges, off-diagonal \verb{~~} rows become
\code{cov} edges, and diagonal \verb{~~} rows are carried as variance
annotations. Intercept and mean rows (\code{~1}) are carried as mean
annotations on the vertex (\code{mean = free}, \code{mean = 0},
\verb{mean = <value>}, or \code{mean = fixed} when lavaan fixes the mean at a
sample value, as for exogenous covariates under \code{fixed.x = TRUE}),
so that a model's mean structure survives the round trip through
\code{\link[=umg_to_lavaan]{umg_to_lavaan()}}. A single person plate is added over all random
vertices, making explicit the replication that classic SEM
diagrams leave implicit.
}

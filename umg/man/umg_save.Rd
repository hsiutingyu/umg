% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{umg_save}
\alias{umg_save}
\title{Save a UMG diagram to a file}
\usage{
umg_save(
  model,
  file,
  width = 6,
  height = 4.5,
  res = 300,
  theme = umg_theme(),
  backend = c("grid", "ggplot"),
  ...
)
}
\arguments{
\item{model}{An object of class \code{umg}.}

\item{file}{Output path; the extension (\code{.pdf}, \code{.png}, \code{.svg},
\code{.tex}/\code{.tikz}, \code{.dot}/\code{.gv}) selects the format.}

\item{width, height}{Dimensions in inches for graphics devices.}

\item{res}{Resolution in dpi for PNG output.}

\item{theme}{A \code{umg_theme} for graphics output (see \code{\link[=umg_theme]{umg_theme()}}).}

\item{backend}{Graphics backend for raster/vector output:
\code{"grid"} (default) or \code{"ggplot"}.}

\item{...}{Passed to \code{\link[=plot.umg]{plot.umg()}} / \code{\link[=umg_ggplot]{umg_ggplot()}} (graphics),
\code{\link[=umg_to_tikz]{umg_to_tikz()}} (TikZ), or \code{\link[=umg_to_dot]{umg_to_dot()}} (DOT).}
}
\value{
The path \code{file}, invisibly.
}
\description{
Renders a UMG to a graphics file (PDF, PNG, or SVG), or writes code
for one of the export backends. The output format is taken from the
file extension: \code{.pdf}/\code{.png}/\code{.svg} produce graphics,
\code{.tex}/\code{.tikz} produce TikZ source, and \code{.dot}/\code{.gv} produce
Graphviz DOT source. Graphics may be produced with either the base
\code{grid} renderer (default) or the \pkg{ggplot2} backend. Graphics
dimensions are in inches.
}
\examples{
umg_save(umg_factor("F", paste0("y", 1:4)),
         tempfile(fileext = ".pdf"))
umg_save(umg_irt("2PL"), tempfile(fileext = ".tex"))
umg_save(umg_irt("2PL"), tempfile(fileext = ".dot"))
}

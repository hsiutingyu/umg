% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/motifs_extra.R
\name{umg_sem}
\alias{umg_sem}
\title{General structural equation model builder}
\usage{
umg_sem(
  measurement,
  structural = list(),
  covariances = list(),
  scaling = c("marker", "variance"),
  observed = character(0),
  badge = c("statistical", "structural"),
  plate_index = "i = 1, ..., N"
)
}
\arguments{
\item{measurement}{Named list mapping each latent factor name to a
character vector of its indicator names.}

\item{structural}{Optional list of length-2 character vectors
\code{c(from, to)} giving directed regressions among factors and/or
observed variables.}

\item{covariances}{Optional list of length-2 character vectors
giving symmetric covariance edges.}

\item{scaling}{\code{"marker"} (fix first loading to 1, the default) or
\code{"variance"} (fix the factor variance to 1; no marker loading).
Under variance scaling, exogenous factors are annotated
\code{N(0, 1)} and endogenous factors carry a fixed unit \emph{residual}
variance (\verb{resid var = 1}), matching the convention of
\code{lavaan}'s \code{std.lv = TRUE}, so every factor's scale is fixed.}

\item{observed}{Optional character vector of additional observed
variables referenced only in \code{structural}/\code{covariances}.}

\item{badge}{\code{"statistical"} (default) or \code{"structural"}.}

\item{plate_index}{Index label for the person plate.}
}
\value{
An object of class \code{umg}.
}
\description{
Assembles a complete SEM from a measurement specification and an
optional structural specification, the flexible route when no
single-purpose motif fits. Factors named in \code{measurement} become
latent continuous vertices; their indicators become observed
continuous vertices with the first loading fixed to one for
scaling (unless \code{scaling = "variance"}). Structural regressions and
covariances are added as \code{dep} and \code{cov} edges respectively.
}
\examples{
m <- umg_sem(
  measurement = list(F1 = c("y1", "y2", "y3"),
                     F2 = c("y4", "y5", "y6")),
  structural  = list(c("F1", "F2"))
)
plot(m)
}

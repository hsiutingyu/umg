% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_model.R
\name{umg_to_lavaan}
\alias{umg_to_lavaan}
\title{Generate lavaan model syntax from a UMG}
\usage{
umg_to_lavaan(model, file = NULL)
}
\arguments{
\item{model}{An object of class \code{umg}.}

\item{file}{Optional path; when supplied the syntax is written there
and the path is returned invisibly.}
}
\value{
A single character string of lavaan syntax (invisibly when
\code{file} is supplied).
}
\description{
Emits \pkg{lavaan} model syntax from a diagram, the inverse of
\code{\link[=umg_from_lavaan]{umg_from_lavaan()}}. The translation uses an explicit, documented
convention. A directed (\code{dep}) edge from a latent vertex to an
observed vertex is a measurement loading (\verb{=~}); every other directed
edge is a regression (\code{~}), so that observed-to-observed paths,
observed-to-latent causes (as in MIMIC), and latent-to-latent
structural paths are all written with \code{~}. Symmetric (\code{cov}) edges
become covariances (\verb{~~}). Fixed edge values are written as
\code{value * variable}; free edges are left for lavaan to estimate, and
residual variances are left implicit. A vertex carrying a mean
annotation in its \code{annot} field (as written by \code{\link[=umg_from_lavaan]{umg_from_lavaan()}}
from a fit with a mean structure) yields an intercept line:
\code{mean = free} becomes \code{v ~ 1}, and \verb{mean = <value>} becomes
\verb{v ~ <value>*1}; \code{mean = fixed} (a mean fixed at a sample value, as
for exogenous covariates under \code{fixed.x = TRUE}) emits nothing,
because a refit fixes it the same way. A growth model therefore
round-trips with its mean structure intact, whether refitted with
\code{lavaan::growth()} or with \code{lavaan::cfa()}.
}
\details{
The convention cannot recover a measurement interpretation of a
latent-to-latent relation (for example a second-order factor loading
on first-order factors), which is written as a latent regression; a
message flags this case so the relevant lines can be changed from
\code{~} to \verb{=~} by hand if a higher-order measurement model is intended.
Mixing (\code{mix}) and deterministic (\code{det}) edges, and the categorical
latent structure of mixture and diagnostic models, have no basic
lavaan syntax and are omitted with a warning.
}
\examples{
cat(umg_to_lavaan(umg_sem(
  measurement = list(F1 = paste0("y", 1:3), F2 = paste0("y", 4:6)),
  structural  = list(c("F1", "F2")))))
}
\seealso{
\code{\link[=umg_from_lavaan]{umg_from_lavaan()}}
}
